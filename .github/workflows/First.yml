name: Sample
on: push

jobs: 
  myjob: 
    runs-on: ubuntu-latest
    steps: 
      - name: Hello Message
        run: echo "Hello"
      
      - name: Get Code Repo
        uses: actions/checkout@v4

      - name: Set-up Node js
        uses: actions/setup-node@v4
        with: 
          node-version: 18

      - name: Install Dependencis
        run: npm install

      - name: Build 
        run: npm run build

      - name: Closed
        run: echo "bye"
